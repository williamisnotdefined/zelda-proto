import type Phaser from 'phaser';

export class PlayerAttackTelegraph {
  constructor(_scene: Phaser.Scene, _x: number, _y: number) {}

  sync(_baseX: number, _baseY: number, _state: string, _direction: string): void {}

  destroy(): void {}
}
